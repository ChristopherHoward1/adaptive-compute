**Findings**

**HIGH** [src/adaptive_compute/benchmark.py]( /Users/cboyfly/Documents/repos/adaptive-compute/src/adaptive_compute/benchmark.py ): The EB benchmark path is no longer the existing v0 path. The diff changes `DEFAULT_BATCH` from `32` to `64` and changes `_tune_params(instrument="eb")` to consider only `B_max=2048` with `b=64`, removing the previous EB candidate set. That violates the plan’s “add, don’t replace,” “released v0 EB path bit-reproducible,” and “`--run` behavior unchanged” requirements. A plain `python -m adaptive_compute.eval --run` now uses the altered EB tuning procedure and can overwrite `work/adaptive-procedure/results.*` with a result from a different search space, which is explicitly out of scope for this unit. The A/B compare can pin `b=64`, but the default EB run path should remain the old behavior except for the accepted A5 `<= alpha` verdict correction.

**LOW** [tests/test_betting.py]( /Users/cboyfly/Documents/repos/adaptive-compute/tests/test_betting.py ): The “drop-in stream equivalence” test mostly proves that both replay decisions can be reconstructed from one separately generated stream. It does not directly capture or compare the actual per-batch deltas consumed inside the two `adaptive_decision` calls. Given the implementation still uses the same `adaptive_bootstrap_stream` and fixed `b`, this is not a concrete behavior bug, but it is weaker than the acceptance wording’s exact `np.array_equal` assertion on matched per-batch prefixes.

Codex verdict: REQUEST CHANGES
